import { Channel, LocalMessage } from 'stream-chat';
export declare enum MessageDeliveryStatus {
    NOT_SENT_BY_CURRENT_USER = "not_sent_by_current_user",
    DELIVERED = "delivered",
    READ = "read",
    SENT = "sent"
}
type MessageDeliveryStatusProps = {
    channel: Channel;
    lastMessage: LocalMessage;
    isReadEventsEnabled: boolean;
};
export declare const useMessageDeliveryStatus: ({ channel, lastMessage, isReadEventsEnabled, }: MessageDeliveryStatusProps) => {
    status: MessageDeliveryStatus | undefined;
};
export {};
//# sourceMappingURL=useMessageDeliveryStatus.d.ts.map