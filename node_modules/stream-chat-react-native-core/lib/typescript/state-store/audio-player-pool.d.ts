import { StateStore } from 'stream-chat';
import { AudioPlayer, AudioPlayerOptions } from './audio-player';
export type AudioPlayerPoolOptions = {
    allowConcurrentAudioPlayback: boolean;
};
export type AudioPlayerPoolState = {
    activeAudioPlayer: AudioPlayer | null;
};
export declare class AudioPlayerPool {
    pool: Map<string, AudioPlayer>;
    allowConcurrentAudioPlayback: boolean;
    state: StateStore<AudioPlayerPoolState>;
    constructor({ allowConcurrentAudioPlayback }: AudioPlayerPoolOptions);
    get players(): AudioPlayer[];
    getOrAddPlayer(params: AudioPlayerOptions): AudioPlayer;
    setActivePlayer(activeAudioPlayer: AudioPlayer | null): void;
    getActivePlayer(): AudioPlayer | null;
    removePlayer(id: string): void;
    deregister(id: string): void;
    clear(): void;
    requestPlay(id: string): void;
    notifyPaused(): void;
}
//# sourceMappingURL=audio-player-pool.d.ts.map